# Lab 8 - K Nearest Neighbors (KNN)

In this lab, I used the K Nearest Neighbors (KNN) algorithm to classify data based on similarity between features. I first explored the dataset by visualizing relationships between variables using plots like pairplots. Then I standardized the features to ensure accurate distance calculations.

Next, I split the data into training and testing sets, trained a KNN model, and evaluated its performance using a classification report. I also tested different values of K to find the optimal number of neighbors and improve the model’s accuracy. The results showed how the choice of K and feature scaling affect the model’s performance.

